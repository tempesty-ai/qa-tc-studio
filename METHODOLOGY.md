# 방법론 — qa-tc-studio

> 핵심 철학: **"AI는 초안 생성, QA는 검증·판단"** — AI 산출물을 초안으로 쓰고 QA가 검증·수정한다.

이 문서는 qa-tc-studio가 테스트케이스(TC)를 만드는 원칙을 설명한다. 도구는 제품에 종속되지 않으며, 아래 원칙을 어떤 웹/앱 제품에도 적용할 수 있다.

## 1. 근거 삼각 대조 (추측 금지)

TC는 상상으로 쓰지 않는다. 가능한 세 소스를 **대조**해서 쓴다.

| 소스 | 용도 |
|---|---|
| ① 명세/매뉴얼 | 기능 설명, 필드·버튼, 화면 흐름 |
| ② 소스 코드 | 정확한 필드명·유효성·**에러 메시지 verbatim**·셀렉터 |
| ③ 실제 앱 | 런타임 동작 검증 (Playwright 등) |

소스에 실제 메시지/로직이 있으면 기대결과에 그대로 옮긴다. 근거가 약하면 TC 대신 **Gap**으로 분리한다(§5). 할루시네이션 TC 금지.

## 2. 구조 — 대메뉴 > 카테고리 > 화면 > 섹션 > TC

- **Depth**는 제품 화면의 메뉴 구조를 따른다 (대메뉴 = Depth1, 카테고리 = Depth2, 화면 = Depth3).
- 각 화면을 **섹션**(목록/등록/수정/삭제/조회/권한/공통)으로 나눠 TC를 배치 → 추적성·커버리지 확보.
- TC ID = `{prefix}-{대메뉴코드}-{카테고리코드?}-{화면코드}-{일련번호}`. flat 메뉴는 카테고리코드 생략.

## 3. 절차는 "일반 사용자 관점" 번호 스텝

테스트 절차는 화면 동작 기준으로, **누구나 따라 할 수 있게** 쓴다.

```
1. ~를 클릭한다.
2. ~를 선택한다.
3. [저장] 버튼을 클릭한다.
```

- 각 TC는 **단독으로 재현 가능**해야 한다 → 절차 맨 앞에 "…화면으로 이동한다" 단계를 자동 삽입(`auto_nav`). 권한/로그아웃 TC는 제외.

## 4. 공통 테스트 패턴 체크리스트

화면마다 아래 패턴을 대입하고 화면 고유 로직만 추가한다.

- **목록**: 진입 렌더/URL, 컬럼 헤더·순서, 0건 빈 메시지, 정렬, 페이징, 검색·필터(단일/복합/미존재/특수문자), 새로고침 후 조건 유지
- **등록**: 필수값 미입력, 길이 초과/미달, 형식검증(IP/포트/이메일/숫자/날짜), 중복키 차단, 정상 등록→목록 반영+성공 메시지, 취소 시 미저장
- **수정**: 기존값 로딩, 변경 저장 반영, 키값 readonly, 동일값 저장
- **삭제**: 확인 팝업, 취소 시 미삭제, 확인 시 삭제+반영, 참조관계(차단/Cascade), 다중 삭제
- **권한**: 관리자/일반 노출차, 읽기전용 버튼 비활성, 미로그인 직접 URL→로그인 리다이렉트
- **공통/비기능**: 세션 만료, 다국어 전환, 뒤로가기, 응답시간

## 5. 설계 기법 태깅 + 위험도/확신도

각 TC에 **사용한 설계기법**을 태깅한다:
`경계값분석(ON/OUT 쌍) · 동등분할 · 결정테이블 · 상태전이 · 조합(pairwise) · 네거티브 · 명세기반(정상)`.
경계값은 유효/무효 짝으로 생성한다.

두 축의 메타를 부여한다 — 값이 없으면 규칙(휴리스틱)이 자동 태깅하고, **최종 판단은 QA가 검토·수정**한다.

- **위험도(High/Med/Low)** = 기능이 잘못됐을 때 업무 영향. 삭제·저장·권한·정상등록 = High 경향.
- **확신도(High/Med/Low)** = 이 TC가 소스대로 정확한지에 대한 신뢰. 소스에 verbatim 메시지/명시 로직 = High; 서버검증·시각판정·외부의존 = Med/Low.
- **활용**: `위험 High + 확신 Low` = 최우선 정밀 검토. 확신도가 낮을수록 QA 검증 비중 ↑.

## 6. Gap Analysis (할루시네이션 방지장치)

소스만으로 기대결과를 특정하기 어려운 항목(임계값 적정성, 외부 연동, 실데이터, 시각/사용성, 성능 SLA, 보안 정책)은 TC로 만들지 않고 **Gap 목록**으로 분리 → QA가 직접 확인·협의.

## 7. 탐색 테스트 차터

TC 밖 영역(대량 데이터, 특수 입력, 동시 편집, 상태 전이, 권한/세션)을 살펴보도록 화면별 탐색 가이드를 제공한다.

## 8. 자체 검증 스크립트 (필수) — AI 자가채점 금지

TC는 `tc_data.json` **단일 소스**로 관리하고, `validate_tc.py`가 검증한다:
중복 ID · 필수 컬럼 누락 · 기대결과 공란 · 절차 스텝 형식 · 위험도/확신도/자동화 값 이상.
**검증은 스크립트가 한다** — AI가 자기 산출물을 스스로 "통과"라고 채점하지 않는다. CI에 물릴 수 있다(error 시 exit 1).

## 9. 산출물

`tc_data.json`(정본) → `render_report.py` →

- **report.html** — 단독 파일. Pass/Fail/N·T·비고를 브라우저 localStorage에 저장.
- **dashboard.html** + `serve_dashboard.py` — 팀 공유. 결과를 서버(statuses.json/notes.json)에 저장해 여러 명이 공유.

리포트 구성: 종합 결과(진행율·집계·자동화 커버리지·위험/확신 분포) / 대메뉴·카테고리별 TC / 설계맵(확신도 히트맵·기능 트리·Gap·탐색차터) / 판정 기준.

## 10. 자동화 경계 (안전)

- **대상은 개발/테스트 환경 한정.** 운영 환경 금지.
- 자동화 생성 데이터는 **접두어(예: `AUTO_TC_`) + 정리(afterEach)**.
- 파괴적 시나리오(대량 삭제·라이선스 갱신·실제 발송)는 `@destructive` 태그로 분리, 기본 실행 제외.
- 실제 메신저 발송·장시간 학습/서빙 잡·오디오·시각 판정 = **수동(자동화 불가)**.
